---
import { format, isDate, parseISO } from "date-fns";

const { date, formatString = "LLLL do, yyyy" } = Astro.props;

const dateToFormat = isDate(date) ? date : parseISO(date);
---

<time datetime={format(dateToFormat, "yyyy-MM-dd")}>
  {format(dateToFormat, formatString)}
</time>
